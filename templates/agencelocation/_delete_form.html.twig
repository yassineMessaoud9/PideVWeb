<form method="post" action="{{ path('app_agencelocation_delete', {'idagence': agencelocation.idagence}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ agencelocation.idagence) }}">
    <button class="btn">Delete</button>
</form>
