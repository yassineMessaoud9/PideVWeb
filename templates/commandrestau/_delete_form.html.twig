<form method="post" action="{{ path('app_commandrestau_delete', {'numCommande': commandrestau.numCommande}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commandrestau.numCommande) }}">
    <button class="btn">Delete</button>
</form>
