<form method="post" action="{{ path('app_locationvoiture_delete', {'idlocation': locationvoiture.idlocation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ locationvoiture.idlocation) }}">
    <button class="btn">Delete</button>
</form>
