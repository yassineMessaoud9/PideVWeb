<form method="post" action="{{ path('app_voitureee_delete', {'matricule': voitureee.matricule}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voitureee.matricule) }}">
    <button class="btn">Delete</button>
</form>
