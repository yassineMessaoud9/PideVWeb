{% extends 'baseadmin.html.twig' %}

{% block title %}Edit Ticket{% endblock %}

{% block body %}
    {{ form_start(form, {attr:{novalidate:'novalidate'} }) }}
    <div class="d-flex mb-3">
        <a class="btn btn-primary mt-3" style="max-height: 35px" href="{{ path('app_ticket_index') }}"><i class="fa-solid fa-circle-left"></i></a>

    </div>
    <h1 style="margin-left: 45%; color: blue">Edit Ticket</h1>
    {{ include('ticket/_form.html.twig', {'button_label': 'Update'}) }}

   {#  <a href="{{ path('app_ticket_index') }}">back to list</a>#}

    {{ include('ticket/_delete_form.html.twig') }}
{% endblock %}
