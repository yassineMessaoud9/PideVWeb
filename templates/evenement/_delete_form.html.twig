<form method="post" action="{{ path('app_evenement_delete', {'ideve': evenement.ideve}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement.ideve) }}">
    <div class="d-flex justify-content-center">
        <button class="btn btn-danger">Delete</button>
    </div>
</form>
