<form method="post" action="{{ path('app_reservationvol_delete', {'idreservationvol': reservationvol.idreservationvol}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservationvol.idreservationvol) }}">
    <button class="btn">Delete</button>
</form>
