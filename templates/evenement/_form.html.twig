{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="d-flex justify-content-center">
        <button class="btn btn-primary h2">{{ button_label|default('Save') }}</button>
    </div>

{{ form_end(form) }}
