{% extends 'baseadmin.html.twig' %}

{% block title %}Commandplat{% endblock %}

{% block body %}
    <h1>Commandplat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commandplat.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commandplat_index') }}">back to list</a>

    <a href="{{ path('app_commandplat_edit', {'id': commandplat.id}) }}">edit</a>

    {{ include('commandplat/_delete_form.html.twig') }}
{% endblock %}
