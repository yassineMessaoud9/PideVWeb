<form method="post" action="{{ path('app_plat_delete', {'idplat': plat.idplat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plat.idplat) }}">
    <button class="btn">Delete</button>
</form>
